# ==============================================================================
# IEC 61499 分布式工业控制系统 —— Python 依赖清单
# ==============================================================================
# 核心（必需）：
Flask>=2.0            # Web 控制台
PyYAML>=5.4           # nodes.yaml 配置解析（缺失时自动退化为内置解析器）

# 推荐（缺失时系统自动降级为"纯共享内存通道"单机运行）：
paho-mqtt>=1.6        # MQTT 跨节点通信通道

# 安装：pip install -r requirements.txt
# ==============================================================================
